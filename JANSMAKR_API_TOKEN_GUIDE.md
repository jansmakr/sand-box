# 🔑 jansmakr@gmail.com API 토큰 생성 가이드

## ✅ **계정 확인 완료!**

### 확인된 프로젝트
```
✅ carejoa-kr-auto (최신)
   - URL: carejoa-kr-auto.pages.dev
   - Git: jansmakr/sand-box
   - 업데이트: 21분 전

✅ carejoa-kr (기존)
   - URL: carejoa-kr.pages.dev
   - Git: 연결 없음
   - 상태: 비활성
```

### 사용할 프로젝트
```
🎯 carejoa-kr-auto
- 최신 프로젝트
- GitHub 연결됨
- 활성 상태
```

---

## 🎯 **API 토큰 생성 단계**

### Step 1: API Tokens 페이지 접속

**현재 위치:**
```
Cloudflare Dashboard → Workers & Pages
```

**이동 방법:**
```
1. 우측 상단 프로필 아이콘 (jansmakr@gmail.com) 클릭
2. "API Tokens" 선택
```

---

### Step 2: Create Token

**버튼 클릭:**
```
우측 상단의 파란색 "Create Token" 버튼
```

---

### Step 3: 템플릿 선택

**선택할 템플릿:**
```
"Edit Cloudflare Workers"
```

**템플릿 찾는 방법:**
```
1. 페이지를 아래로 스크롤
2. "Custom token" 섹션 찾기
3. "Edit Cloudflare Workers" 템플릿 찾기
4. "Use template" 버튼 클릭
```

---

### Step 4: 권한 확인 및 추가

**기본 권한 (이미 선택됨):**
```
✅ Account - Workers Scripts - Edit
✅ Account - Account Settings - Read
```

**추가해야 할 권한:**

#### 4-1. Cloudflare Pages 추가
```
1. 권한 목록에서 "+ Add more" 클릭
2. 첫 번째 드롭다운: "Account" 선택
3. 두 번째 드롭다운: "Cloudflare Pages" 선택
4. 세 번째 드롭다운: "Edit" 선택

결과: Account | Cloudflare Pages | Edit ✅
```

#### 4-2. D1 추가
```
1. 다시 "+ Add more" 클릭
2. 첫 번째 드롭다운: "Account" 선택
3. 두 번째 드롭다운: "D1" 선택
4. 세 번째 드롭다운: "Edit" 선택

결과: Account | D1 | Edit ✅
```

#### 4-3. Workers R2 Storage 추가 (선택사항)
```
1. 다시 "+ Add more" 클릭
2. 첫 번째 드롭다운: "Account" 선택
3. 두 번째 드롭다운: "Workers R2 Storage" 선택
4. 세 번째 드롭다운: "Edit" 선택

결과: Account | Workers R2 Storage | Edit ✅
```

---

### Step 5: 최종 권한 체크리스트

**생성 전 확인:**
```
✅ Account - Workers Scripts - Edit
✅ Account - Cloudflare Pages - Edit
✅ Account - D1 - Edit
✅ Account - Account Settings - Read
✅ Account - Workers R2 Storage - Edit (선택)
```

---

### Step 6: Account Resources 설정

**기본 설정:**
```
Zone Resources:
  Include | All zones

Account Resources:
  Include | All accounts
```

**그대로 유지하면 됩니다!**

---

### Step 7: 토큰 생성

**진행 순서:**
```
1. 페이지 아래로 스크롤
2. "Continue to summary" 버튼 클릭
3. 권한 요약 확인
4. "Create Token" 버튼 클릭
```

**중요:**
```
⚠️ 토큰은 한 번만 표시됩니다!
⚠️ 반드시 안전한 곳에 복사하세요!
```

---

### Step 8: 토큰 복사

**표시 형식:**
```
Your API Token:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
lbERodP9vM98sSkAxU6fYOAvugOSHPDbjkiiwnLn
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

**복사 방법:**
```
1. "Copy" 버튼 클릭
   또는
2. 토큰 텍스트 전체 선택 → Ctrl+C (또는 Cmd+C)
```

**저장:**
```
복사한 토큰을 메모장이나 안전한 곳에 임시 저장
```

---

## ✅ **완료 후 해야 할 일**

### GenSpark Deploy 탭 업데이트

**단계:**
```
1. GenSpark AI로 돌아가기
2. "Deploy" 탭 클릭
3. "Cloudflare API Token" 입력란 찾기
4. 복사한 토큰 붙여넣기
5. "저장" 버튼 클릭
```

---

## 🚀 **배포 명령어**

**토큰 업데이트 후 실행:**
```bash
cd /home/user/webapp
npm run build
npx wrangler pages deploy dist --project-name carejoa-kr-auto
```

**예상 결과:**
```
✨ Deployment complete!
🌍 https://carejoa-kr-auto.pages.dev
```

---

## 📋 **체크리스트**

```
□ 프로필 아이콘 클릭
□ API Tokens 선택
□ Create Token 클릭
□ "Edit Cloudflare Workers" 템플릿 선택
□ Cloudflare Pages 권한 추가
□ D1 권한 추가
□ Workers R2 Storage 권한 추가 (선택)
□ Continue to summary
□ Create Token
□ 토큰 복사 및 저장
□ GenSpark Deploy 탭 업데이트
□ 배포 실행
```

---

## 💡 **FAQ**

### Q: 권한 추가 버튼이 안 보여요
**A:** 페이지를 아래로 스크롤하세요. "+ Add more" 버튼은 기존 권한 목록 아래에 있습니다.

### Q: 토큰을 복사하지 못했어요
**A:** 토큰은 한 번만 표시됩니다. 다시 생성하려면:
1. API Tokens 페이지로 돌아가기
2. 방금 생성한 토큰 삭제
3. 다시 Create Token부터 시작

### Q: 어떤 프로젝트 이름을 사용하나요?
**A:** **carejoa-kr-auto** 사용
- 최신 프로젝트
- GitHub 연결됨
- 활성 상태

---

## 🎯 **지금 바로 시작하세요!**

```
1. 우측 상단 프로필 아이콘 클릭
2. "API Tokens" 선택
3. "Create Token" 클릭
```

**토큰 생성 후 바로 알려주세요!** 🚀

---

**작성일**: 2025-12-28  
**계정**: jansmakr@gmail.com ✅  
**프로젝트**: carejoa-kr-auto ✅  
**우선순위**: 🔴 최우선
